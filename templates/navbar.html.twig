<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('program_index') }}">Programs</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('wild_index') }}">All Séries</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('show_category', { 'categoryName': "horror" }) }}">Horror</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('show_category', { 'categoryName': "action" }) }}">Action</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('show_category', { 'categoryName': "adventure" }) }}">Adventure</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('show_category', { 'categoryName': "animation" }) }}">Animation</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('show_category', { 'categoryName': "fantastic" }) }}">Fantastic</a>
            </li>
        </ul>
        {% if app.user %}
            <a href="{{ path('app_logout') }}"> Se déconnecter</a>
        {% else %}
            <a href="{{ path('app_login') }}"> Se connecter</a>
        {% endif %}
    </div>
</nav>
